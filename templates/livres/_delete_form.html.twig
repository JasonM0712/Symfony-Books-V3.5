<form method="post" action="{{ path('app_livres_delete', {'id': livre.id}) }}" onsubmit="return confirm('êtes vous sur de supprimer ce livre?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livre.id) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
