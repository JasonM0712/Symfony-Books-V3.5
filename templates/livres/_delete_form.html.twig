<form method="post" action="{{ path('app_livres_delete', {'id': livre.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livre.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
